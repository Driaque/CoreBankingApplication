@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper,
    IList<Core.BlogTest.Tag> tags)
{
foreach (var tag in tags)
{
        <div class="tags-div">
            @MVCTut.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
}
}